@using PagedList
@using PagedList.Mvc
@model IPagedList<TH.WebUI.ViewModels.ProjectIndexViewModel>

@{
    ViewBag.Title = "工程招标或发包";
}
@section Styles{
    @Styles.Render("~/Content/PageList")
}
<div class="panel panel-default">
    <div class="panel-heading"><h2>工程招标或发包信息</h2></div>
    <div class="panel-body">
        <p>...</p>
    </div>
    <ul class="list-group">
        @foreach (var j in Model)
{
    <li class="list-group-item">
        <a href="@Url.Action("Details", new { id = j.Id })" target="_blank" data-original-title="
            <h4>@j.Title</h4>
            <ul class='list-unstyled'>
                <li>项目名称:@j.ProjectName</li>
                <li>企业或经营个体:@j.Company</li>
                <li>招标或发包内容:@j.Content</li>
                <li>工程开始时间:@j.StartTime</li>
                <li>工程期限:@j.TimeLimit</li>
                <li>发布时间:@j.CreateDate</li>
            </ul>">
            @j.Title
        </a>
    </li>
}
    </ul>
</div>
@Html.PagedListPager(Model, page => Url.Action("Index", new { pageIndex = page }))
<p>
    @Html.ActionLink("我要发布", "Create", null, new { @class = "btn btn-primary" })
</p>
@section scripts
{
    <script type="text/javascript">
        $("a").tooltip({
            placement: "right",
            html: true
        });
    </script>
}
